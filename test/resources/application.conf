ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [com.saga.authenticator.ApplicationKt.module]
  }
}

redis {
  url = "redis://localhost:50000"
  password = null
}

mongo {
  connectionString = "mongodb://localhost:50001/test"

}
